[project]
name = "generatively-pretrained-transformer"
version = "0.1.0"
description = "GPT from scratch"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.10.0",
    "jupyter>=1.1.1",
    "ipykernel>=7.2.0",
]
